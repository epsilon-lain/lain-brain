# Lain Brain North Star

**Lain Brain 是一个以用户自己的自然语言为第一等知识表示的个人认知系统。**

它的目的不是把用户的思想翻译成标准术语、数学符号或 AI 的语言，而是：

> **保留用户原本的表达，同时让 AI、形式系统、现实证据和已有知识体系围绕这些表达建立可追踪的协议连接。**

### 1. 用户自己的语言是本体

最终知识笔记只包含：

- 用户在聊天中自己说出的最终结论；
- 用户自己对这些结论的自然语言解释；
- 用户后来亲自修改过的文字。

例如用户说：

> 一个复数加上0还是它自己。

那么主笔记就可以仍然是：

> **一个复数加上0还是它自己。**

不会自动被改成：

> 对任意 z∈C，z+0=z。

更不会被 AI 的解释替换。

AI 的语言不是用户知识的正文。

---

### 2. AI 对用户的理解永远只是“目前的假设”

AI 不存在某个瞬间可以宣布：

> “我已经确定你是什么意思。”

它只能保持：

> **“根据目前的上下文，我暂时以为你是这个意思。”**

因此理解过程是持续进行的：

```
用户表达
   ↓
AI 当前理解 H₀
   ↓
继续聊天 / 举例 / 推演 / 实验
   ↓
获得新的信息
   ↓
H₀ 仍然适用？
   ├── 是 → 继续使用
   └── 否 → 微调成 H₁
                  ↓
               继续……

```

理解不是审批流程。

没有必要每句话都出现：

```
[对，就是这个意思]
[不对]

```

正常情况下直接继续交流。

---

### 3. 只有真正阻碍理解的歧义才打断用户

AI 不应该为了“形式安全”不停要求确认。

只有当不同解释会导致真正不同的后续结果，而且现有上下文无法判断时，才问。

例如：

> “它最后会变成0。”

如果“它”可能指：

- 数列项；
- 部分和；
- 极限；

而不同解释会改变推理，那么才应该问：

> 这里的“它”指哪一个？

所以原则是：

> **确认不是步骤，消除真正的歧义才是。**

---

### 4. SemanticSpec 是 AI 当前的 meaning hypothesis，不是用户知识本身

SemanticSpec 可以记录 AI 当前认为：

- 哪些是对象；
- 哪些是变量；
- 哪些是关系；
- 哪些是运算；
- 哪些是定义；
- 哪些是命题；
- 哪些部分仍然不确定。

例如：

```
梦到(A, B)

```

完全可以保留“梦到”这个名字。

不需要为了形式化而立刻写成：

```
R(A,B)

```

SemanticSpec 的目标是：

> **忠实表达当前理解。**

而不是：

> **尽快进入 Lean。**

---

### 5. 用户可以创造自己的概念和世界

用户不需要先知道已有数学叫什么，才有资格思考一个概念。

例如：

> 无穷物件时间尺度

它可以先作为一个真正的新概念存在。

然后逐渐定义：

```
τ：某种无穷过程中的时间尺度

X(τ)：对象在这个尺度中的状态

问题：
什么叫收敛？
什么叫速度？
能否定义 dX/dτ？
能不能有某种几何？
如果 τ 是复数会怎样？

```

AI 可以发现它和：

- partial sums；
- asymptotics；
- dynamical systems；
- nonstandard analysis；

之类已有理论有相似之处。

但只能说：

> “这和 X 很像。”

不能偷偷变成：

> “你说的就是 X。”

**类比可以由 AI 提出，等同必须来自后续理解。**

---

### 6. 用户定义的世界不需要现实世界批准

Lain Brain 必须区分：

```
Definition
Assumption
Custom-world rule
Conjecture
Derived consequence
Open question
Empirical claim
Analogy

```

比如用户定义：

> 在这个世界里存在一种时间 τ。

这不是现实物理命题。

AI 不应该说：

> “现实物理没有这个，所以错误。”

它应该问：

> 在这个规则世界内会推出什么？

---

### 7. Lean 只是一个可选 backend

Lean 很重要，但不是 Lain Brain 的中心。

对于：

> 一个复数加上0还是它自己。

AI 当前理解可以映射为：

```
∀ z : ℂ, z + 0 = z

```

于是 Lean 可以验证：

```
在这个 formal interpretation 下，
该命题可以被证明。

```

这形成一个：

```
LeanProtocolArtifact

```

挂在用户原话旁边。

但：

> A梦到B。

可能根本不需要 Lean。

> 无穷物件时间尺度。

也不应该因为 Lean 暂时表达不了，就失去作为知识的资格。

所以：

```
User knowledge
      │
      ├── Lean backend
      ├── empirical backend
      ├── numerical experiment
      ├── custom-world reasoning
      ├── textbook mapping
      └── nothing

```

全部合法。

---

### 8. Lean 验证的是解释，不是用户本人

如果：

```
用户：
一个复数加0还是自己

```

AI 当前假设：

```
H₀：
标准复数加法

```

Lean 验证：

```
∀ z : ℂ, z + 0 = z

```

那么真正得到的是：

> **在解释 H₀ 下，这个命题通过 Lean 验证。**

如果后来用户说：

> 这里的 + 其实是我自己定义的运算。

那么不是 Lean “错了”。

而是：

```
H₀ 不再是当前理解
↓
H₁ 出现

```

旧 Lean artifact 仍然可以作为：

> `tested_under: H₀`

的历史记录。

---

### 9. “理解”与“是真的”必须分开

例如：

> 天下乌鸦一般黑。

AI 完全可以理解这句话：

```
semantic understanding ✓

```

然后现实证据发现反例：

```
empirical truth ✗

```

这是两个不同维度。

不是：

```
Lean error

```

而可能是：

```
UserConclusion
    │
    └── CounterexampleProtocolArtifact

```

所以：

> **理解一句话，不意味着同意它是真的。**

---

### 10. 用户知识图谱和 Protocol Graph 是两层东西

用户真正看到的可以只是：

```
“复数加0还是自己”
       │
       └── “0是加法单位元”

“无穷物件时间尺度”
       │
       ├── “什么是这个尺度下的速度？”
       └── “复时间是否有意义？”

```

这是 **User Knowledge Graph**。

后面隐藏着：

```
Protocol Graph

UserNode A
 ├── SemanticSpec H₄
 ├── LeanArtifact
 ├── textbook mapping
 └── evidence

UserNode B
 ├── SemanticSpec H₂
 └── CustomWorldArtifact

```

外部表示围绕用户知识生长。

不是反过来。

---

### 11. AI 可以主动联想，但不能擅自决定关系

AI 可以发现：

> “复数加0还是自己”

可能和：

> “0是加法单位元”

有关。

于是：

```
AI proposes edge

```

但 AI 的联想只是 proposal。

真正属于用户知识图谱的关系，应当来自后续聊天中用户自然接受、使用或明确认可。

也就是说：

> **AI 可以帮助长出神经连接，但不能偷偷给脑子接线。**

---

### 12. Candidate Note 是最后的整理，不是思考发生的地方

正确顺序是：

```
Chat
↓
不断理解 / 推演 / 纠正 / 实验
↓
逐渐形成稳定的用户结论
↓
UserConclusion
↓
Candidate Note

```

Candidate Note 的工作只是：

- 找出用户已经说清楚的结论；
- 保留用户原话；
- 组织结构；
- 建立关系；
- 连接已有 protocol artifacts。

它不应该到最后才突然决定：

> “原来用户的意思是这个。”

真正的思考发生在聊天过程中。

---

### 13. Lain Brain 会逐渐“认识”用户，但不是把用户冻结成规则

长期使用以后，它可以积累：

```
过去用户这样说时通常是什么意思
这个词以前被怎样使用
这个自定义概念有哪些规则
哪些表达之间经常有关

```

这些形成越来越好的 **semantic priors**。

但它们永远可以被新的对话推翻。

所以：

```
过去的理解
≠
永久定义

```

而是：

```
更好的初始猜测

```

Lain Brain 越来越懂用户，不代表越来越有资格定义用户。

恰恰相反：

> **它越懂，就应该越少打断，越少擅自改写。**

---

## 最终闭环

整个系统最后希望做到的是：

```
           用户自己的语言
                  │
                  ▼
        AI 当前的意义理解
                  │
       ┌──────────┼──────────┐
       ▼          ▼          ▼
     Lean       世界证据     自定义理论
       │          │          │
       └──────────┼──────────┘
                  ▼
             新的理解
                  │
                  ▼
             继续聊天
                  │
                  ▼
         用户自己的语言

```

也就是：

> **用户用自己的语言思考。**
>
> **AI 尝试理解这种语言。**
>
> **不同系统帮助测试这种理解。**
>
> **测试结果反过来帮助 AI 更好地理解用户。**
>
> **但最终知识依旧属于用户自己的语言。**

如果必须只留下三句话，我会留这三个：

> **用户的话是本体，其他语言都是协议。**
>
> **AI 对用户意义的理解永远只是当前最好的假设。**
>
> **Lain Brain 的目标不是把一个人的思想标准化，而是让外部系统学会和这个人的思想交流。**

这个就可以当以后每次架构改动之前拿出来看的“北极星”了喵
